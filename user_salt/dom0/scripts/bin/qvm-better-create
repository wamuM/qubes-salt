#!/usr/bin/env python3

# This script creates AppVMs allowing to specify features they should have.
# for instance you can create a coding appvm (which automatically sets the template to
# template-coding and applies custom states).

import sys
import os

def usage():
    print("[?] Usage: qvm-better-create <name> [stereotype] [features...]")
def main():
    if len(sys.argv) < 2:
        print("[!] Incorrect Parameters")
        usage()
        sys.exit(1)
    
    name = sys.argv[1] 
    
    # load stereotypes from vm-stereotypes 
    # apply selected one
    


if __name__ == "__main__":
	main()
